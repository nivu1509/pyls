[project]
name = "pyls"
version = "0.1.0"
description = "a simple exercise to mimic some behaviours of the `ls` command."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pytest>=8.4.1",
]
